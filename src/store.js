import {createStore} from 'redux'

const reducer =( state, action)=>{
    switch(action.type){
        case "add":
        break;
        case "substract":
        break;
    }

const store = createStore(reducer, 1);

}
